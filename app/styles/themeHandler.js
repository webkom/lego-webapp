/*

const updateTheme = () => {
  // Change Abakus logo
  // Change variable in footer (color)
  setTheme(user.theme);
};

const setTheme = theme => {
  document.documentElement.setAttribute('data-theme', theme);
};

*/
